'use client';

import { AlertTriangle, ShieldAlert } from 'lucide-react';
import { Button } from '@/components/ui/Button';

export function Einstellungen({ password, message, loading, onPasswordChange, onReset, onExport, onImport }: { password: string; message: { type: 'success' | 'error'; text: string } | null; loading: boolean; onPasswordChange: (value: string) => void; onReset: () => void; onExport: () => void; onImport: (file: File) => void }) {
  return <div className="mx-auto max-w-xl space-y-6"><div className="rounded-xl border border-slate-200 bg-white p-6"><h2 className="mb-4 text-xl font-bold">Datenverwaltung</h2><div className="flex flex-col gap-3 sm:flex-row"><Button variant="secondary" onClick={onExport}>Daten exportieren</Button><label className="cursor-pointer rounded-lg bg-slate-100 px-4 py-2 text-center font-semibold text-slate-700 hover:bg-slate-200">Daten importieren<input type="file" accept="application/json" className="hidden" onChange={(event) => { const file = event.target.files?.[0]; if (file) onImport(file); event.target.value = ''; }} /></label></div></div><div className="rounded-xl border border-red-200 bg-white p-6"><h2 className="mb-4 flex items-center gap-2 text-xl font-bold text-red-600"><ShieldAlert className="h-5 w-5" />Saison zurücksetzen</h2><input type="text" value={password} onChange={(event) => onPasswordChange(event.target.value)} placeholder="Tippe 'Schachturnier' zum Bestätigen" className="mb-3 w-full rounded-lg border bg-slate-50 px-4 py-2 text-sm" /><Button variant="danger" disabled={loading} onClick={onReset} className="flex w-full items-center justify-center gap-2"><AlertTriangle className="h-4 w-4" />Alle Partien & Punkte löschen</Button>{message && <p className={`mt-3 text-sm ${message.type === 'error' ? 'text-red-600' : 'text-emerald-600'}`}>{message.text}</p>}</div></div>;
}